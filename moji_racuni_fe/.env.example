# Frontend API base URL (without trailing slash)
# Use localhost if backend is local-only, or your host machine IPv4 when accessing from other devices.
REACT_APP_BASE_URL=http://127.0.0.1:8000

# Optional: disable source maps in production/local builds
GENERATE_SOURCEMAP=false
